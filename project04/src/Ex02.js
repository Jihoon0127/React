import React from 'react'
import menu from "./menu.json"
import MenuItem from './components/Ex02MenuItem'
function App() {
    // let name1 = list.map(item => item.name);
    // let price1 = list.map(item => item.price);
    // let content1 = list.map(item => item.content);
    // let imgSrc1 = list.map(item => item.imgSrc);


}

const Ex02 = ({name,price,content,imgSrc}) => {
    
  return (
    <div>
        <MenuItem/>   
        
    </div>
  )
}

export default Ex02